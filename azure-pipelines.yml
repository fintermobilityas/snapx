strategy:
  matrix:
    linux:
      imageName: 'ubuntu-16.04'

pool:
  vmImage: $(imageName)

variables:
  imageName: 'snapx:$(build.buildId)'

steps:
- script: build.ps1 -Target Bootstrap -DockerAzureImageName $(imageName)
  displayName: 'docker build'